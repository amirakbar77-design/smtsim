# Connection header for proxied WebSockets: "upgrade" when the client asked for
# one, "close" otherwise. Lives in the http block, which is why it is a separate
# file mounted into conf.d's parent.
map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}
