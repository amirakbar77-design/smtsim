# smtsim

Discrete-event simulation of an SMT assembly line, plus an HTTP service and a
web replay UI. See README.md for the architecture and the reasoning behind it.

## Commits

Do not include a `Co-Authored-By` trailer or a "Generated with Claude Code"
line in commit messages.

Conventional commits, one logical commit per unit of work.
